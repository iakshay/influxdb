machine:
    services:
        - docker

dependencies:
    cache_directories:
        - "~/docker"
    override:
        - ./test.sh save

test:
    override:
        - bash circle-test.sh:
            parallel: true
